# MBTI 王 · 微信小程序上传凭证（登记）

## 正式小程序（默认上传目标）

| 项 | 值 |
|----|----|
| AppID | `wx3d15ed02e98b04e3` |
| AppSecret | **仅本机** `miniprogram/.env` 内 `WX_MINIPROGRAM_APPSECRET`（已加入 `.gitignore`，勿提交） |

## 工程内对应位置

- **开发者工具 / 上传识别的小程序**：`miniprogram/project.config.json` 字段 `appid`（已与上表一致）。
- **Secret 与脚本读取**：`miniprogram/.env`（从 `miniprogram/.env.example` 复制后填写）。

## 上传

在微信开发者工具中打开目录 `miniprogram/`，版本管理 → 上传；或使用 CLI（本机已安装工具时）按微信文档指定 `--project` 为该目录。

## 轮换 Secret 后

在公众平台重置 AppSecret 后，同步更新本机 `miniprogram/.env`，**不要**把 Secret 写进任何会进 Git 的文件。
