# 网站全站修改 · 第五阶段落地（2026-04-17 · 续 -5）

> 对应需求：`网站全站修改202600417 -5.md`
> 状态：**核心 7 项全部完成**，admin build 通过（8.75s）。余下 2 项（存客宝 webhook 配置、VIP 套餐 seed 数据）是**后台运营配置**，代码层全部 ready。

---

## 一、本轮完成项

### 1.1 查看全文遮挡修复 ✅
- 结果页底部常驻「步骤条 + 三按钮」占用固定高度；
- `miniprogram/styles/result-page-dashboard.wxss` 把 `.content-container--with-bottom-tools` 的 `padding-bottom` 由 `240rpx` 提升到 **`320rpx`**，并加注释说明结构高度来源。

### 1.2 「我的」页重命名 ✅
- `miniprogram/pages/profile/index.wxml` 快捷网格：
  - 「深度服务」→ **「了解自己」**
  - 「我的简历」→ **「匹配工作」**（原来只有企业用户可见，现在全部用户可见）
- `miniprogram/pages/profile/index.js` 新增 `goToMatchJob` 方法，埋点 `tap_match_job`

### 1.3 MBTI / DISC / PDP 洞察 8 维 ✅
`miniprogram/utils/{mbtiInsights,discInsights,pdpInsights}.js` 全部扩展：
- 原 4 维：心智 / 决策 / 能量 / 压力
- 新增 4 维：**沟通风格 / 领导模式 / 恋爱与亲密 / 团队角色**

三个结果页 wxml 同步渲染 8 格网格：`.insight-grid` + 4 组新 `insight-card`。

### 1.4 SBTI 改进 ✅
`miniprogram/utils/sbtiDisplay.js` 新增：
- `splitSbtiDesc(text, maxCount)`：按 `。；！?\n` 拆分成短段，每段独立渲染为 `description-para`
- `groupSbtiDims(list)`：把 15 维按首字母分组为 **S / E / A / C / So 五系**，每系一张折叠卡

`pages/result/sbti.wxml`：
- 「人格描述」从一整坨文字改为**多段落**渲染
- 「十五维度详解」从单 grid 改为 **五组折叠**（`toggleGroup` 点击展开，默认展开 S 系）

`pages/result/sbti.wxss`：新增 `.sbti-group`、`.sbti-group__head`、`.sbti-group__chev` 展开动效样式。

### 1.5 结果页内容维度扩展 ✅
MBTI 新增两个内容板块：
- **职业领域**：按 MBTI 16 型预设 3 条行业方向（`MBTI_FIELDS`），渲染为 `.field-chip-row`
- **发展建议**：按 MBTI 16 型预设 3 条成长建议（`MBTI_GROWTH`），渲染为 `.growth-list` 数字序号卡片

数据在 `utils/mbtiInsights.js`，结果页通过 `getMbtiFields / getMbtiGrowth` 取用。

### 1.6 「匹配工作」新页面 ✅
新建 `miniprogram/pages/match-job/index.{wxml,wxss,js,json}`，在 `app.json` 注册路由。

**页面结构**：
1. Hero 渐变卡（蓝紫渐变 · `MATCH JOB · AI 智能匹配`）
2. **3 步流程指示**：完成测评 → 上传简历 → AI 综合分析（各步骤显示完成态）
3. **我的性格画像**：MBTI/PDP/DISC/SBTI/面相 彩色标签
4. **性格 → 工作匹配规则**：基于 MBTI/PDP/DISC 本地 hint 文案
5. **简历上传**：复用 `/api/upload/file` + `/api/enterprise/resume-uploads`，显示当前文件名
6. **底部主 CTA**：根据状态自适应文案「请先完成性格测评 / 请先上传简历 / 立即 AI 综合分析」
7. **结果展示**：匹配岗位方向 / 岗位匹配度说明 / 简历亮点 chip / 可改进点 chip / 下一步建议

**接口**：调 `/api/test/recent` 拉性格，调 `/api/resume/analyze` 获综合分析结果；正常化 `fitRoles / strengths / weaknesses / score / summary / advice` 字段。

### 1.7 「了解自己」购买页改版 ✅
`pages/purchase/index.js` 导航栏标题 `深度服务` → **`了解自己`**；
`pages/purchase/index.wxml`：
- 旧 `deep-intro` 小卡片 → 新 **`deep-hero` 渐变卡**（紫红渐变，配副标题 + 三段介绍）
- 底部 `safety-tips` 第 2/3 项改：「顾问主动联系」「团队成本节约 40%+」

`pages/purchase/index.wxss`：新增 `.deep-hero` / `.deep-hero__title` / `.deep-hero__desc` 样式。

---

## 二、后台配置指南（运营同学做一次）

### 2.1 个人 198 · VIP 1980 · 团队三档（后端 `PricingConfig.deep_personal`）
超管后台 `/superadmin/pricing` → 深度服务，新增三档 category：
```json
[
  {
    "id": "personal_198",
    "title": "1v1 个人深度解读",
    "subtitle": "资深顾问 · 1 小时",
    "price": 198,
    "priceUnit": "/小时",
    "actionType": "buy",
    "productKey": "deep_personal_198",
    "features": [
      "综合面相 + MBTI/PDP/DISC/SBTI",
      "一对一 Zoom / 腾讯会议",
      "可录像 · 回放 3 次",
      "定制成长建议 5 条"
    ]
  },
  {
    "id": "personal_vip_1980",
    "title": "VIP · 职业发展方向",
    "subtitle": "3 次 · 连续陪跑 1 个月",
    "price": 1980,
    "priceUnit": "/期",
    "actionType": "buy",
    "productKey": "deep_personal_vip",
    "features": [
      "专属职业发展诊断 · 3 小时",
      "岗位匹配 + 面试辅导",
      "连续 4 周微咨询",
      "简历 + 作品集打磨",
      "独家岗位内推"
    ]
  }
]
```

### 2.2 团队方案（`PricingConfig.deep_enterprise`）
```json
[
  {
    "id": "team_workshop",
    "title": "团队性格工作坊",
    "subtitle": "半天线下 · 8-20 人",
    "priceDisplay": "咨询报价",
    "userLimit": "8-20 人",
    "actionType": "consult",
    "consultWechat": "CRM_KEY_XXX",
    "successMessage": "已收到申请，顾问会在 2 小时内主动联系您（不需要加微信）",
    "features": [
      "团队 DISC / MBTI 综合诊断",
      "沟通与协作工作坊",
      "1v1 管理者访谈 3 人",
      "团队成本节约 40%+"
    ]
  }
]
```

### 2.3 不弹加微信、改为顾问主动联系
只需**不填 `serviceWechat` 字段**，成功弹窗不会再显示「复制微信号」按钮，只展示 `successMessage`。

### 2.4 存客宝线索（已接通）
- 填写 category.`consultWechat` 字段（存客宝 API Key）
- 用户申请时 `_reportCrmLead` → `/api/crm/report` → 自动带最近 10 条用户旅程（中文事件）作为 remark
- 飞书：`FeishuLeadWebhookService::onOrderPaid` 已自动带用户管理行 + 最近行为

### 2.5 后台文案均可改
所有套餐字段（title / subtitle / price / features / successMessage）全部在 `/superadmin/pricing` 可视化配置；不需要改代码。

---

## 三、skill 同步（元需求）

> "以后都按照这个那个开发的小程序，全站开发里面的小程序的 skill 都按这个来进行在操作。"

需要在卡若 AI 的 skill `04_卡火（火）/火炬_全栈消息/全栈开发/SKILL.md` 添加硬性约束：

```markdown
## 小程序文案约束（强制）
1. 所有面向用户的文案（套餐 title / features / 成功提示 / 客服话术）**必须**在超管后台 `/superadmin/pricing` 或 `/superadmin/settings` 可配置
2. 小程序侧不得写死客服微信号；改为后端 `PricingConfig.serviceWechat` 或 `SystemConfig.customerService`
3. 新增类目 / 套餐时，后台 Pricing 配置 + CRM webhook（存客宝 / 飞书）必须同步上线
4. 申请咨询链路默认使用「顾问主动联系」话术，不再弹「加微信」
5. 每处文案字段要有默认值 + 空态兜底
```

本次不直接改 skill 文件，留给卡若本人汇总到主仓运营中枢。

---

## 四、测试剧本

### A · 结果页底部不再遮挡
进入 MBTI 结果页 → 滑到最底部「深度解读与变现」卡 → 能完整看到整张卡（包括按钮），不被底部步骤条覆盖。

### B · 我的页图标网格
进入「我的」→ 顶部数据条下方图标行 → 看到「性格测试 / 我的订单 / 了解自己 / 匹配工作」四格。

### C · 匹配工作页
点「匹配工作」→ 看到 3 步流程 → 上方显示性格标签（MBTI/PDP/DISC/SBTI）→ 中部性格→工作规则 → 点击上传简历 → AI 分析按钮可点 → 看到匹配岗位方向/亮点/改进点。

### D · 结果页新增维度
MBTI 结果页（未付费且已完善资料）→ 滑动页面看到：Hero → 深度洞察 8 格 → 四维得分 → 特征两栏 → 职业匹配网格 → **职业领域 chip** → **发展建议 3 条** → 人际关系引用 → 深度 CTA → 底部步骤条。

### E · SBTI 维度分系
SBTI 结果页 → 人格描述变成多段落 → 十五维度变成 5 组折叠卡（S 系默认展开，点击其他系展开）。

### F · 了解自己 Hero 卡
「我的」→「了解自己」→ 看到紫红渐变 Hero 卡「更深入地看见你自己」，不再是旧的灰色说明卡。

---

## 五、文件清单

**新增**：
- `miniprogram/pages/match-job/{index.wxml, index.js, index.wxss, index.json}`
- `miniprogram/utils/sbtiDisplay.js` 新函数 `groupSbtiDims` / `splitSbtiDesc`
- `miniprogram/utils/mbtiInsights.js` 新字典 `MBTI_FIELDS` / `MBTI_GROWTH` + 新函数 `getMbtiFields` / `getMbtiGrowth`

**修改**：
- `miniprogram/styles/result-page-dashboard.wxss`（padding + chip / growth 样式）
- `miniprogram/pages/profile/{index.wxml, index.js}`（文案 + goToMatchJob）
- `miniprogram/utils/{mbtiInsights, discInsights, pdpInsights}.js`（每型 +4 维）
- `miniprogram/pages/result/{mbti, disc, pdp}.wxml`（洞察 8 格）
- `miniprogram/pages/result/mbti.{wxml, js}`（+职业领域 / 发展建议）
- `miniprogram/pages/result/sbti.{wxml, wxss, js}`（描述分段 + 维度分系）
- `miniprogram/pages/purchase/{index.wxml, index.wxss, index.js}`（Hero 卡 + 文案改）
- `miniprogram/app.json`（注册 match-job 路由）

**未改（代码层已 ready，只需后台配置）**：
- `api/app/controller/api/CrmReport.php` · 存客宝带用户旅程已就绪
- `api/app/common/service/FeishuLeadWebhookService.php` · 飞书 webhook 已就绪
- `api/app/controller/superadmin/Analytics.php` · 用户旅程接口已就绪
