# 开发环境配置
# 留空：请求走当前站点相对路径 /api/v1，由 vite.config.ts 代理到本机 PHP（默认 127.0.0.1:8787），避免依赖 hosts。
# 若直连远程 API，再改为完整地址，例如：https://mbtiapi.quwanzhi.com
VITE_API_BASE_URL=https://mbtiapi.quwanzhi.com

# 环境标识
VITE_APP_ENV=development
VITE_APP_TITLE=MBTI管理后台-测试环境
